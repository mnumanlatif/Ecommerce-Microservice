{
  "name": "ecommerce-backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n auth,cart,product,payment -c yellow,blue,magenta,green \"npm --prefix auth-service run dev\" \"npm --prefix cart-service run dev\" \"npm --prefix product-service run dev\" \"npm --prefix payment-service run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
